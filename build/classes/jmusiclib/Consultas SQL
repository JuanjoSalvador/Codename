LISTAR CANCIONES DE UN DISCO DETERMINADO

- Ordenado por tracks

select track, tituloCancion
from cancion
where tituloDisco in (SELECT tituloDisco FROM `disco` WHERE tituloDisco = 'Lupus Dei')
order by track

LISTAR DISCOS DE UN AUTOR DETERMINADO

- Ordenado por año de lanzamiento

select tituloDisco, fechaLanza
from disco
where nombreGrupo in (SELECT nombreGrupo FROM grupo WHERE nombreGrupo = /*Autor*/)
order by fechaLanza

LISTA TODOS LOS DISCOS CON SU AUTOR

select tituloDisco, nombreGrupo
from disco
where nombreGrupo in (SELECT nombreGrupo 
                      FROM grupo)

LISTA UN DISCO DETERMINADO CON SU AUTOR (en bucle sirve para listar todos los discos)

select cancion.track, cancion.tituloCancion, disco.tituloDisco, grupo.nombreGrupo  
from grupo, disco, cancion
where grupo.nombreGrupo = disco.nombreGrupo and disco.tituloDisco = cancion.tituloDisco and disco.tituloDisco = 'Lupus Dei'
ORDER BY `cancion`.`track`

BÚSQUEDA POR SIMILITUD

SELECT tituloCancion FROM cancion
where tituloCancion like '%ira%' ORDER BY tituloDisco
